# Toy Model Inference

## Setup

Virtual Environment 

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Azure

```bash
brew update && brew install azure-cli
az login
```

ML Flow
    
```bash
mlflow server
```

## Training

## Inference